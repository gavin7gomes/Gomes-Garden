import React from 'react';
import { Helmet } from 'react-helmet';

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name='description' content={description} />
      <meta name='keyword' content={keywords} />
    </Helmet>
  );
};

Meta.defaultProps = {
  title: 'Welcome To Gomes Garden',
  description: 'We sell best organic products for cheap',
  keywords: 'vegetables, fruits, organic, farm, cheap, healthy food',
};

export default Meta;
